# parsing = require('../lib/parsing')

# describe 'parser', ->
#   it 'can handle the syntax described', ->
#     @ast = parsing('spec/fixtures/sample.dub')
#     console.log JSON.stringify(@ast, null, 2)
